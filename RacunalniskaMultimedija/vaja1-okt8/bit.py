class Bit:
    def __init__(self):
        self.bits = []

    # Read bits from byte value
    def read_bits(self, byte_value):
        bit_array = []

        # TODO: Add byte to bit magic

        self.bits.append(bit_array)

    # Write bits from bit array
    def write_bits(self, filepath):
        return

    def find_bit_positions(self, bit_array, bit_to_find):
        return

    def find_and_replace_bit(self, old_bit_value, new_bit_value):
        return

    def get_bits(self):
        return self.bits
